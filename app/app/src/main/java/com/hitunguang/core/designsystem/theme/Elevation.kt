package com.hitunguang.core.designsystem.theme

import androidx.compose.ui.unit.dp

object Elevation {
    val none = 0.dp
    val low = 0.dp // Rely on subtle borders or color differences instead of shadows
    val medium = 1.dp // Previously 2dp
    val high = 2.dp // Previously 4dp
    val extraHigh = 4.dp // Previously 8dp
}
